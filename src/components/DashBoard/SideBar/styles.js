const styles = (theme) => ({
    drawerPaper: {
        width: 240,
        height: '100%',
        marginBottom: -2000, /* any large number will do */
        paddingBottom: 2000,
        position: "relative",
        zIndex:99
      },
});

export default styles;
