const styles = theme => ({
    backgroundForm: {
        background: theme.palette.primary.main,
        display: "flex",
        padding: 40,
        flexDirection: "column",
        alignItems: "center",
        justifyContent: "center",
        minHeight: "100vh",
        textAlign: "center"
    },
    button: {
        marginTop: 15,
        marginBottom: 15
    },
    textField: {
        marginTop: 15
    }
});

export default styles;
