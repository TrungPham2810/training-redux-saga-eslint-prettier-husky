const styles = () => ({
    fab: {
        display: "flex",
        alignItems: "center",
        justifyContent: "flex-end"
    },
    taskItem: {
        marginBottom: "10px"
    }
});

export default styles;
